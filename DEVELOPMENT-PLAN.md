# 大头鹰开发计划与迭代记录

## v1.8：工作视觉与 AI 接入体验修正

日期：2026-09-06；发布目录 `dist/v1.8.0`。

1. 重做工作桌前后遮挡，保留角色、手和电脑的空间关系；用完整前挡板遮住原来的断开脚部。实心火焰新循环同时修正缩放轴心，原火焰与桌子源图保留。
2. 新绘制正常闭眼、紧张闭眼关键姿势，将普通眼和无瞳黄色眼分隔在不同插帧片段中；重做 WorkToBusy 91 帧、BusyExit 121 帧，保持阶段时长、动作完整端点和 60 Hz 调度。
3. 一键接入窗口：Codex、Claude Code、CodeBuddy Code 的用户级配置预览、MCP + Stop Hook 安装、备份、幂等更新、失败回滚和只移除自己的设置。自定义根路径不明、已有手动集成、密文或危险配置形状时明确停止；不绕过 Hook 信任或组织策略，不配置真实账号。
4. 荣誉九图独立生成，三套动作浮雕各配铜银金材质，取消彩色鹰头和外贴系列标识；原生圆形裁切、立体边缘、阴影、悬停及间歇扫光。系统减少动画、关闭、隐藏、失焦时停止动画时钟。
5. 验证包括完整新序列视觉/锚点/透明边缘检查、实际 WPF 场景和荣誉布局、新窗口真实点击、隔离安装回滚及实际 PowerShell/Git Bash Hook 到命名管道。发布版另外回归喂食、办公、GitHub 和 MCP；真实 AI 客户端审核/回复送达、物理显示器长期帧率仍由实机验收补齐。

保持本地养成与现有 GitHub 功能，不添加聊天读取、模型调用、远程 Hook 或自动迁移用户手动配置。后续动作场景、换肤、更多解锁仍是后续范围。

---

## v1.7：GitHub 通知、分级荣誉和桌宠菜单

日期：2026-09-06。发布目录：`dist/v1.7.0`。本轮三个目标均已实现：

1. GitHub 直连：桌宠内置 REST 轮询，不依赖 AI 调用 MCP。可选 classic PAT 连接，当前 Windows 用户 DPAPI 保存凭据；首次静默基线、持久化版本去重、默认相关通知过滤、未读列表和红点、消息气泡、严格 GitHub URL 白名单，以及按通知版本标记本地已读。遵守服务器轮询间隔、条件请求和退避。断开先保存禁用状态，再独立清理凭据；失败提示明确，不假装清理成功。
2. 荣誉：保留旧三项荣誉的解锁兼容性，展示三条路线各铜银金三档，共 9 枚。独立荣誉窗口支持全部／已获得／未解锁、进度与窄窗口布局；只从养成快照推导，不改奖励或存档版本。ImageGen 绘制包含大头鹰元素的三档徽章，界面另叠加路线标识。
3. 菜单：用原生 WPF 主题重绘为奶油色、暖金和鼠尾草绿；摸头、喂饭、打哈欠、工作操作平铺。保留大小子菜单和键盘操作；置顶开关同步已打开的面板和气泡。GitHub 红点仅是打开消息入口，不抢占角色拖动区域。
4. 验证：GitHub 离线测试 46 组、荣誉逻辑 12 项、荣誉实际 WPF 布局 18 项，原逻辑/存档/动画回归 82 项；实际发布 EXE 再做新界面、GitHub 模拟联调、日常喂食、持续办公和 MCP 协议验证。完整证据及真实账号、人工多屏、物理 60 FPS 等未验证边界见 [验证记录](docs/VALIDATION-v1.7.md)。

不做 GitHub 写操作、OAuth 应用注册、Webhooks 中转服务、企业自建域名支持或实际 AI 客户端配置。真实 GitHub 连接由用户在本机授权验证，不读取其现有凭据。后续动作与场景路线不变。

---

## 历史 v1.6：动作分组、持续工作和主动碎碎念

日期：2026-09-05。当前实现与发布目标目录：`dist/v1.6.0`。以下描述本轮实现；下方 v1.5 初版计划及验证结论按原版本保留。

1. 分组与调度：待机只包含站立、打哈欠。交互中的摸头对应害羞、喂食对应干饭，完整播放后站立 2 秒。原丢炸弹和白眼素材保留在工程历史资源中，但不进入当前自动待机池。AI 通知仅说话，不触发换动作。
2. 持续工作：用户选择开始工作，预载资源后进入工位场景；桌子滑入、电脑落下，小鹰开始循环办公。连续工作满 30 分钟后，在安全端点进入转忙片段，再无限循环忙碌中，不穿插站立。取消工作或饱食度到 0 时，在完整阶段边界衔接对应退场，恢复站立。
3. 工作结算：工作期间总消耗为饱食度 12/小时、心情 10/小时；每分钟额外 +1 经验，挂机粮食积累照常。按实际时间结算，不依赖动画帧数；在饱食度 0 的精确时刻截断工作收益。喂食、摸头需先取消工作，退出场景前不消耗这些交互资源。
4. 保存与恢复：保存工作标志、连续/累计时长和不足一分钟的经验进度。重启、离线或长时间挂起最多补算 2 小时；未耗尽饱食度则入场恢复工作，不重复记账。
5. 碎碎念：60 条精简职场句子，本地随机、每分钟一次，轮完一组再打乱；右键和养成面板提供持久化开关。气泡缩小、去掉名字栏、贴近鹰头。已有消息、拖动、暂停或长时间挂起时不积压补播。
6. 动作资源：角色、桌子、电脑和火焰分层，场景单独进入/循环/转忙/退出。ImageGen 提供关键姿势与道具，RIFE 光流生成过渡帧并校准脚点；六段角色共 786 帧，火焰循环 121 帧，不宣称是 907 次独立绘画。保留原角色比例，不把整只鹰当转场道具平移或翻转。
7. 验证：本轮已运行 38 项 Core 确定性测试及 10 项生产场景运动测试。覆盖分组、60 Hz 端点、工作安全边界、循环不回站立、30 分钟阈值、饥饿截断、保存恢复、道具进退场和火焰衔接。本计划不将代码测试当成本轮发布版 GUI、人工指针拖动或长期物理屏幕 60 FPS 验收；这些以本轮实际验证记录为准。

暂不扩展 AI 长时记忆、多角色换肤、联网养成、商城；趴桌哭、更多动作等级解锁与成就继续按后续迭代推进。

---

## 历史：v1.5 初版开发计划

日期：2026-09-05。原始参考库包含 39 个 GIF，制作时保持原件不动；获准使用的原件可自行放在仓库根目录的 `References/Memes/`，这是可选本地素材，不随公开仓分发。

当前进展：以下首版闭环已实现并发布到 `dist/v1.5.0`。26 项逻辑、7 项真实存档、4 项素材回归通过；实际 GUI 与 MCP 联调通过。具体证据与未验证边界见 `docs/VALIDATION-v1.5.md`。

## 本次交付：先打通可以玩的闭环

1. 保留 C# / .NET 8 / WPF、原生拖动、多屏 DPI、现有四套 121 帧动画。
2. 分离养成状态、行为队列、帧资源清单、桌面窗口和外部通知桥。
3. 本地养成：饱食度、心情、粮食、简单经验等级；初始粮食 5；每 5 分钟积累 1 份，存储上限 99；单次离线结算最多 2 小时。没有死亡或重度惩罚。
4. 交互：单击摸头，拖动小鹰，打开状态面板，一键或拖拽喂食；检查冷却、库存、饱食度；本地原子存档。
5. 新增完整的全身干饭动画，保持造型、脚点、透明边缘。保留完整动作后站立 2 秒。先验收这一套再铺开全部动作。
6. MCP：独立 stdio 适配器固定 AI 来源，通过当前 Windows 用户专用命名管道连接桌宠；通知气泡显示来源和消息。返回应用由用户本地配置，外部消息不能执行任意命令。
7. 验证：原动画 12 项回归、养成/队列/持久化边界、真实 MCP 握手和工具调用、本机发布启动；旧 v1.4 产物保留。

## 后续迭代（不宣称在首版完成）

- 趴桌哭场景：出现桌子 → 趴下 → 哭泣循环 → 喂食反应 → 擦泪起身 → 收桌。需要新绘制的 enter / loop / exit 动画，不能用 GIF 方块或突然换图凑数。
- 根据 GIF 目录逐批新增动作、等级解锁和趣味成就；先保证每套动作质量。
- 各 AI 客户端的自动回复完成 hook，按客户端支持情况逐个接入。MCP 本身不是通用聊天监控器。
- 暂不做长时 AI 记忆、完整多角色换肤、联网账户、付费经济、商城。

## 验收界限

- 60 Hz 播放和素材采样不等于保证所有机器实测恒定 60 FPS。
- 任何新动作必须完整过渡、全身可见、不改变角色身份、不漂移；不使用整图翻转或交叉淡化冒充动作。
- 程序、素材、代码均留在本项目；本次不创建仓库、不提交公司父仓库、不改 AI 客户端配置。
