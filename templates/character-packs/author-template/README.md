# 作者模板，不是可运行模型

此目录只包含 `character-package.json` 字段示例。没有 `.moc3`、纹理、真实参数绑定或可编辑模型。

使用方法与安全/验证边界见 [CHARACTER-PACKS.md](../../../docs/CHARACTER-PACKS.md)。复制到自己的目录后填入实际模型路径、参数、挂点和服装，先执行 `--mode author-template`；模板通过不代表 `RuntimeReady`。

源模型制作与授权均须自行完成；不要用占位文件或修改扩展名绕过制作步骤。当前工具只做离线数据验证，不开启生产宠物的模型导入功能。
