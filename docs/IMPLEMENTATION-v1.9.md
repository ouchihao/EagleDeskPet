# REQ-001～008 实施记录

开始：2026-09-19。基线提交：`46cd0df`，基线版本 v1.8.0。

用户授权连续完成八项需求，每完成一个可验证的小项 commit 一次。本文记录真实进度和验收，不以代码存在或测试数量代替动画效果验收。

## 交付小项

- [x] 核对工作区、Git 身份与需求范围，基线 38 项确定性测试通过。
- [x] REQ-006 第一小项：工资、钱包显示、旧档迁移、原子保存及交易基础（后续商店接入单独验收）。
- [x] REQ-008 第一小项：默认工作场景配置化、道具槽与异步换装安全边界（新素材和商店接线后续验收）。
- [x] REQ-002 / 007 第一小项：统一内容拥有权、成长奖励、购买事务和装备状态基础（商店界面与素材另项验收）。
- [x] REQ-003 第一小项：猜拳状态机和独立窗口（真实动作及桌宠独占接线另项验收）。
- [x] REQ-001 / 004 第一小项：情绪策略、饥饿场景状态机和完整动作调度（素材/UI 接线另项验收）。
- [x] REQ-008 第二小项：整套服装清单、全帧校验、异步安全换装与可释放的按需缓存（真实办公服组合另行验收）。
- [x] REQ-005：按任务更新的通知协议、列表、去重和过期语义（最终整机回归仍待全量阶段）。
- [x] REQ-001 / 004 动画小项：饥饿三段、护头龇牙、喝茶和五种猜拳动作关键图及完整逐帧素材（整机联调另项）。
- [ ] REQ-003：猜拳逻辑、短局交互、结果动作与退出处理。
- [ ] REQ-007 / 008：新动作、第二套桌子和电脑、服装适配及组合视觉验收。
- [ ] 全量回归、隔离 GUI 验证、发布打包、文档与交付证据。

## Demo 决策

- 虚拟货币称为“鹰币”，初始 0；每完整有效工作分钟 +1，上限 999999，保留不足一分钟进度。
- 隐藏和锁屏不暂停养成；离线计薪沿用最多 2 小时并在饥饿耗尽时截断。v1 旧档迁移不追溯此前工资。
- 商店采用本地游戏币，无真实支付；扣款与授予拥有权一起原子保存，失败不消耗余额。
- 桌子与电脑只改变外观；所有基础动作始终免费，新动作重复播放不扣币、不额外发奖。
- 新情绪场景默认关闭；饥饿进入阈值 25、退出阈值 35，低心情进入 25、退出 40；自动场景有冷却和时长上限。
- 猜拳不下注、不消耗资源，宠物先选拳；奖励为少量心情且有冷却，不奖励金币或经验。
- 保留现有 C# / WPF、最高 60 Hz 时间轴及原地脚点，不以全图淡化或翻转代替肢体过渡。

商品、服装与新动作素材必须经过实际预览才能标记交付。素材生成使用内置 imagegen，关键图、提示词和离线插帧工具保留在仓库；失败时不悄悄切到需要付费 API 的后备路径。

## 验证记录

### 基线

- 命令：`dotnet run --project DuckDeskPet.SelfTest/DuckDeskPet.SelfTest.csproj -c Release`
- 结果：38 / 38 通过。
- 未修改真实用户存档、GitHub Token 或 AI 客户端配置。

### 工资与存档基础

- 新增 EconomySelfTest：43 / 43；覆盖有效工作、两小时上限、精确饥饿截断、迁移不补薪、零头、回拨、高水位、交易幂等、失败回滚和双进程存档竞争。
- 原 Core 38 / 38、PetStore 7 / 7、WorkStage 10 / 10 回归通过。
- 原子交易先在候选存档中扣币与授予效果，磁盘写入成功后才更新界面使用的状态；错误不覆盖原存档。
- 饭搭子面板显示鹰币与下一枚所需有效工作秒数，明确免费粮食不受影响。
- 此处不宣称商店商品、衣服或全部需求已验收。

### 工作场景配置化

- 默认场景 `work.default`、桌子 `desk.default`、电脑 `computer.default` 使用受校验的 JSON 描述。
- 整套道具先预载，再于非工作画面或下一次入场起点生效；活动工作循环中不突然换桌，渲染回调不读图。
- 新 SceneCatalogSelfTest 24 / 24，原 WorkStageSelfTest 10 / 10。
- 实际 WPF WorkSceneV2SelfTest 4 项断言和 16 张快照通过；办公遮挡、闭眼转黄眼和火焰锚点已人工查看。
- 这一步只验证默认资源配置化，尚不代表全部服装或商品组合完成。

### 内容与奖励基础

- 三个默认内容永久免费；薄荷桌 20 鹰币 / 第 10 顿饭赠送、深夜电脑 30 鹰币、喝茶动作 15 鹰币 / Lv.2 赠送、办公服装 40 鹰币。
- 内容目录、购买及荣誉使用相同 ID；只有通过资源就绪检查的内容才允许购买，代码中的商品条目不等于素材已交付。
- 奖励向老玩家补授内容，不补发金币；丢图或未知内容保留所有权，显示回退状态。
- ContentSelfTest 54 / 54；Economy 43 / 43、Core 38 / 38、PetStore 7 / 7、Honor 12 / 12 回归通过。
- 猜拳心情奖励的跨重启冷却字段已经持久化，完整小游戏另行验收。

### 猜拳核心和窗口

- 23 / 23 逻辑与真实 WPF 控件测试通过；宠物先选拳，准备、出拳和结果均等待演出回执。
- 十秒左右一局，二十秒超时取消；关闭、暂停、加载失败不发奖励，收尾完成后才释放窗口。
- 每五分钟最多心情 +2，无金币、经验或下注。桌宠真实动画接线仍在进行，未以模拟委托替代最终验收。

### 情绪与场景调度

- EmotionSelfTest 49 / 49；原 Core 38 / 38、WorkStage 10 / 10 回归通过。
- 默认只自动哈欠；情绪场景需打开开关，迟滞阈值和五分钟冷却避免重复打扰。连续十秒内第三次摸头会烦躁，但不额外奖励属性。
- 饥饿场景含进场、最多两次循环、退场；喂食/工作/关闭开关等待完整退场。暂停冻结场景帧，不瞬移回站立。
- 手动动作仍遵守完整收尾和两秒站立间隔；关闭情绪只清自动请求，不吞掉用户交互。

### 按任务传话

- 新协议、真实命名管道、实际 MCP SDK stdio、CLI、并发/限流共 90 项检查通过，旧 MCP 桥接回归通过。
- 桌宠右键可进入 AI 任务列表；未读、静音、清理、返回应用均接入。主 WPF Release 构建零警告、零错误。
- 按来源+任务 ID+递增 revision 跟踪，过期不伪称成功；Stop Hook 仍仅表示新回复。任务正文仅保留内存，不写入存档或状态接口。
- GitHub 通知继续使用既有独立通道，不将 PR 更新推断为 CI 或 AI 任务成功。

### 整套服装与缓存

- OutfitSelfTest：89 / 89 隔离用例，另对真实默认资源完整解码通过。整个服装套必须覆盖默认全部动作；缺帧、坏图、跨套路径、帧长/尺寸错误均不可购买或使用。
- 默认三动作预热，新动作按需加载；换装仅在站立安全边界提交，异步最新请求优先。
- 暂缺保存的服装时明确回退默认并保留所有权/选择，不在单动作中偷偷脱衣。
- `ReleaseClips` 释放猜拳与喝茶临时帧，保护当前实际显示动作；旧加载完成不能回填已释放缓存或误删新请求。
- 办公服艺术资产与整机组合仍须单独验收，此基础测试不代表所有美术已交付。

### 新增全身动作

- 十段、1150 张 RGBA 帧：饥饿进场/循环/退场共 303；喝茶、护头龇牙各 121；五种猜拳演出共 605。
- 全部逐帧 QA 通过，检查透明边缘、脚点、时长与真实变化帧；根代理查看所有新动作深色背景联系图。
- 饥饿段落首尾精确一致，普通动作精确回到原始中性帧；默认待机池仍只哈欠。
- 本地 GIF 使用 ffmpeg 连续帧作为表演参考；生成要求、原始生成图、关键帧和离线工具均保留，原表情包不进入发行包。

### 可替换工位与独立小剧场

- 薄荷桌、午夜电脑已经生成并登记兼容槽；人物、前后桌层、电脑和火焰独立，非工作场景边界才提交替换。
- 修正午夜电脑使用近透明画布范围而缩小的问题：只按可见 alpha 物体区域对齐，保持 102px 宽和桌面锚点。
- 严格构建审计与运行时缺可选图降级分开：缺可选物品只禁用该款，默认必需资源缺失仍明确报错。
- ExpansionIntegrationSelfTest 14 / 14，含 303 张饥饿帧、独立预览窗口、深浅背景、缩放、完整收场和资源释放；SceneCatalog 24 / 24、旧 WorkSceneV2 4 项回归通过。
- 独立小剧场不读写照料、余额或装备，支持自然收场、暂停/继续、重播，隐藏停止渲染。

### 窗口接线与第一轮全应用验收

- 商店、荣誉奖励、情绪场景、真实猜拳演出均接入同一个桌宠；本次启动已赚金额与消费后的余额分开显示。
- 完整办公装 19 段 / 2299 帧已生成，通过全资源审计；1150 张新默认动作帧、源图、关键帧、提示词和制作工具均已保留。
- 第一轮真实 EXE 隔离联调 8 / 8 通过（60.61 秒），覆盖购买去重、预览无副作用、任务正文不落盘、连续摸头、饥饿先退场再喂食一次、收工后换设备、猜拳奖励冷却与办公服整套切换。
- 用户追加桌子入场穿身优化、更多设备/服装：正在补语义遮挡和第二轮 9 项全应用验收；第一轮结果不替代新增资源验收。

### 更多商店外观（追加项）

- 新增暖木复古桌 25、闪电电竞桌 35、奶油复古电脑 25、闪电小电脑 35 鹰币；摸鱼卫衣 45 鹰币，完整资源通过后才能上架。
- 当前目录总计 12 项：4 桌、4 电脑、3 整套形象、1 可手动播放动作；价格和权益继续走统一原子事务。
- 薄荷桌加入实体前围板，不再暴露火焰平直底边；新桌统一 306×96 可见边界，电脑保持 102px 宽、底部 y=275。
- 新增原图和完整提示词位于 `Assets/AnimationSources/shop-expansion-v2-prompts.md`；新变体为真实生成道具，不用代码画简化替身。
- ContentSelfTest 54 / 54；容量用例改为遍历全部可购商品，避免随着新增内容漏测满收藏容量。
